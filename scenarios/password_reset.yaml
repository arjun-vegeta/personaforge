name: account-password-reset
scenario: reset_password
agent_id: YOUR_AGENT_ID

personas:
  - non_tech_user

steps:
  - request_password_reset
  - provide_phone_digits
  - request_alternative_email_delivery
  - confirm_reset_sent

policy:
  file: policies/security_rules.md
